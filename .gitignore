venv/
__pycache__/
*.pyc
instance/*.db
instance/*.db-shm
instance/*.db-wal
uploads/*
!uploads/.gitkeep
# Ad-hoc local test scripts written while developing a feature (not part of
# the deployed app, and never uploaded to Render) - kept out of the repo so
# they don't clutter it or get mistaken for something that ships.
smoke_test*.py
document_templates_data/
build/
dist/
*.spec
NeverlankApp.exe
.env
# Offline pip wheels bundled only for the Windows/.exe distribution zip -
# not needed for cloud hosting (Render installs from PyPI directly), and
# keeping it out of git keeps the repo small and pushes fast.
wheelhouse/
